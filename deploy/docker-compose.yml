name: jarvis-family

services:

  # ── JARVIS ──────────────────────────────────────────────────────────────
  jarvis:
    build:
      context: ../
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file: .env
    volumes:
      - jarvis_data:/app/data
      - chronicle_snapshots:/chronicle-snapshots
    environment:
      CHRONICLE_SNAPSHOT_DIR: /chronicle-snapshots
      CHRONICLE_API_URL: http://chronicle:5174
      GHOSTWRITR_BASE_URL: http://ghostwritr:3000
      # DB_PASSWORD feeds a literal postgresql:// URL here, so deployment
      # inputs must keep that value URL-safe unless this topology changes.
      CATALYST_DB_URL: postgresql://${DB_USER:?DB_USER is required}:${DB_PASSWORD:?DB_PASSWORD is required}@postgres:5432/jarvis_catalyst
      JARVIS_HOME_DB_URL: postgresql://${DB_USER:?DB_USER is required}:${DB_PASSWORD:?DB_PASSWORD is required}@postgres:5432/jarvis_home
      # Background agent scheduler intentionally paused pending redesign of the
      # work-queue/agent-assignment model (2026-07-07). Flip to "true" to resume.
      JARVIS_BACKGROUND_AGENTS_ENABLED: "false"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - internal

  # ── CHRONICLE ───────────────────────────────────────────────────────────
  chronicle:
    build:
      context: ../../chronicle
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file: .env
    volumes:
      - chronicle_snapshots:/app/chronicle-data
    environment:
      # DB_PASSWORD is interpolated directly into this URL and must remain
      # URL-safe for the tracked compose form.
      CHRONICLE_DATABASE_URL: postgresql://${DB_USER:?DB_USER is required}:${DB_PASSWORD:?DB_PASSWORD is required}@postgres:5432/chronicle
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - internal

  # ── GHOSTWRITR ──────────────────────────────────────────────────────────
  ghostwritr:
    build:
      context: ../../ghostwritr
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - internal

  # ── POSTGRES ────────────────────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    env_file: .env
    environment:
      POSTGRES_USER: ${DB_USER:?DB_USER is required}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD is required}
      POSTGRES_DB: ghostwritr
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:?DB_USER is required}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - internal

  # ── REDIS ───────────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - internal

  # ── NGINX ───────────────────────────────────────────────────────────────
  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - jarvis
    networks:
      - internal

  # ── CLOUDFLARE TUNNEL ───────────────────────────────────────────────────
  cloudflared:
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    command: tunnel --no-autoupdate run
    environment:
      TUNNEL_TOKEN: ${CLOUDFLARE_TUNNEL_TOKEN}
    depends_on:
      - nginx
    networks:
      - internal

networks:
  internal:
    driver: bridge

volumes:
  jarvis_data:
  chronicle_snapshots:
  postgres_data:
  redis_data:
