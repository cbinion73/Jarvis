events {
    worker_connections 1024;
}

http {
    # Security headers
    add_header X-Frame-Options DENY;
    add_header X-Content-Type-Options nosniff;
    add_header Referrer-Policy strict-origin-when-cross-origin;

    # JARVIS
    server {
        listen 80;
        server_name jarvis.teambinion.org;

        location / {
            proxy_pass http://jarvis:8787;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_read_timeout 300s;
            proxy_send_timeout 300s;
            client_max_body_size 50M;
        }
    }

    # Chronicle — uncomment when chronicle service is running
    # server {
    #     listen 80;
    #     server_name chronicle.teambinion.org;
    #     location / {
    #         proxy_pass http://chronicle:5174;
    #         proxy_http_version 1.1;
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection "upgrade";
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_read_timeout 300s;
    #     }
    # }

    # Ghostwritr — uncomment when ghostwritr service is running
    # server {
    #     listen 80;
    #     server_name ghostwritr.teambinion.org;
    #     location / {
    #         proxy_pass http://ghostwritr:3000;
    #         proxy_http_version 1.1;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_read_timeout 300s;
    #         client_max_body_size 50M;
    #     }
    # }
}
