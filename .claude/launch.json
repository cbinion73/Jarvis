{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "jarvis",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["-c", "set -a && source .env && set +a && export SSL_CERT_FILE=$(.venv/bin/python -c 'import certifi; print(certifi.where())') && exec .venv/bin/python -m jarvis serve --host 127.0.0.1 --port 8787"],
      "port": 8787
    }
  ]
}
